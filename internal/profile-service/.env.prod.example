# Profile Service Production Configuration
# Copy this file to .env.prod and configure with your production values

# Server Configuration
PROFILE_SERVICE_PORT=8081
NODE_ENV=production

# Database Configuration
# For production, use PostgreSQL. Leave empty for in-memory database (not recommended for production)
PROFILE_SERVICE_DATABASE_URL=postgresql://profile_user:profile_password@postgres:5432/profile_service

# CORS Configuration
CORS_ORIGINS=https://todoapp.jinxcodes.ai,https://aichat.jinxcodes.ai

# Cookie Configuration
COOKIE_DOMAIN=.jinxcodes.ai

# Credits Configuration
DAILY_BONUS_AMOUNT=10
INITIAL_CREDITS_AMOUNT=100

# Production Logging
LOG_LEVEL=info
ENABLE_REQUEST_LOGGING=false
ENABLE_ERROR_DETAILS=false

# Instructions:
# 1. Copy this file to .env.prod
# 2. Configure the database URL with your production PostgreSQL credentials
# 3. Adjust CORS origins if you have additional domains
# 4. Keep this file secure and never commit it to version control
