# NPM Configuration for Storybook
# Ensures proper module resolution for Storybook in Node.js environment

# Use legacy peer deps to avoid conflicts
legacy-peer-deps=true

# Prefer exact versions for stability
save-exact=true

# Disable package-lock for this specific package since we're using Deno primarily
package-lock=false

# Enable hoisting for better module resolution
hoist=true
